version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - cd back-end
      - npm install
  build:
    commands:
      - echo "Build started on `date`"
      - mv back-end/dist/* .
      - rm -rf back-end/

artifacts:
  files:
    - '**/*'