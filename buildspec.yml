version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - cd back-end
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - back-end/dist/**/*
