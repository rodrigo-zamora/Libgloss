version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - echo "Build started on `date`"
      - cd back-end
      - npm install
      - npm run build
  build:
    commands:
      - pwd
      - ls
      - cd ..
      - pwd
      - rm -rf libgloss/
      - rm -rf images/
      - ls
      - mv back-end/* .
      - ls
      - rm -rf back-end/

artifacts:
  files:
    - '**/*'